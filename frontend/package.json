{
  "name": "@cca/frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Chat widget: one continuous thread with inline product + transaction-preview cards (Phase 6).",
  "scripts": {
    "dev": "npx serve src -l 4173",
    "test": "node --test"
  },
  "dependencies": {
    "@cca/contracts": "*"
  }
}
